<?xml version="1.0" encoding="utf-8"?>
<!-- Zgjims Work -->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Background Image -->
    <ImageView
        android:id="@+id/backgroundImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/back_mob" /> <!-- Replace with your background image -->

    <!-- Foreground Content -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:background="#80000000"> <!-- Semi-transparent dark overlay -->

        <!-- App Logo -->
        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="248dp"
            android:layout_height="232dp"
            android:layout_marginBottom="16dp"
            android:contentDescription="HomeSync Logo"
            android:src="@drawable/homesync_logo" />

        <!-- App Name -->
        <TextView
            android:id="@+id/appName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="HomeSync"
            android:textSize="28sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="48dp" />

        <!-- Login Button -->
        <Button
            android:id="@+id/loginButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Log In"
            android:backgroundTint="#1C1C1C"
            android:textColor="#F5F5DC"
            android:layout_marginBottom="16dp" />

        <!-- Register Button -->
        <Button
            android:id="@+id/registerButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Register"
            android:backgroundTint="#F5F5DC"
            android:textColor="#1C1C1C"
            android:layout_marginBottom="0dp"/>

    </LinearLayout>

</FrameLayout>
    <!-- Zgjims Work -->